# Local development only. Lambda uses an IAM role instead of access keys.
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-climate-lens-bucket
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
